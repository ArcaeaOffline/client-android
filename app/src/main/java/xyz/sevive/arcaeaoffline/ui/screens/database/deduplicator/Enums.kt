package xyz.sevive.arcaeaoffline.ui.screens.database.deduplicator

internal enum class GroupByValue {
    SCORE, PURE, FAR, LOST, MAX_RECALL, DATE, CLEAR_TYPE, MODIFIER
}

internal enum class AutoSelectMode {
    PROPERTIES_PRIORITY, R30_PRIORITY, IDENTICAL
}
